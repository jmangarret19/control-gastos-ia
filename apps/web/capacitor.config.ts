import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jmangarret.controlgastos',
  appName: 'ControlGastos',
  webDir: 'dist'
};

export default config;
